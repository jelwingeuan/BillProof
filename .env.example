# Local-only sample target. Do not point the MVP at an arbitrary remote host.
BILLPROOF_TARGET_URL=http://127.0.0.1:4100
BILLPROOF_PROVIDER_URL=http://127.0.0.1:4101
# The dashboard runs on 3000 by default. No credentials are needed for this demo.
